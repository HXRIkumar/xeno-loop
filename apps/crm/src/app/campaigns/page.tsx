import { ComingSoon } from "@/components/coming-soon";

export default function CampaignsPage() {
  return (
    <ComingSoon
      title="Campaigns"
      description="Create, approve, fire — and watch the funnel update live."
      note="Campaigns, the fire endpoint, and the live funnel arrive in Phase 3."
    />
  );
}
